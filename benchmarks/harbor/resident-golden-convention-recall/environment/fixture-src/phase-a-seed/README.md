# Phase A Workspace

The project keeps behavior in `src/golden_convention/price_rules.py`.
Generated expected outputs in `generated/expected_totals.json` are protected
snapshots used by local checks.
