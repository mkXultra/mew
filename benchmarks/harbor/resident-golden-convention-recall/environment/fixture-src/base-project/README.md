# Golden Convention Base Project

This fixture family uses tiny Python modules under `src/golden_convention/`.
Generated JSON files under `generated/` are protected snapshots. Product
behavior changes belong in the source modules, not in generated expected-output
files.
