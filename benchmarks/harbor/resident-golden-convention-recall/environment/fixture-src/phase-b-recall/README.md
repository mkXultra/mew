# Phase B Workspace

The project keeps behavior in `src/golden_convention/eta_rules.py`.
Generated expected outputs in `generated/expected_delivery.json` are protected
snapshots used by local checks.
