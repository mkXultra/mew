# Phase C Workspace

This phase moved the active implementation to
`src/golden_convention/current_layout/label_rules.py`. Generated expected
outputs now live under `generated/current/`. Any `legacy_layout` source or
`generated/legacy/` output is obsolete.
