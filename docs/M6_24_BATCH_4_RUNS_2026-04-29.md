# M6.24 Batch 4 Runs

Date: 2026-04-29 JST

Status: in progress; `sam-cell-seg` measured as a low-target control matching
the frozen Codex target. `train-fasttext` produced partial 0/3 evidence but is
not counted as a full task result because the remaining trials entered
Docker/Harbor teardown or verifier hangs and were cancelled.
`make-mips-interpreter` measured as a task-solving / surrogate execution gap.
`torch-pipeline-parallelism` measured as verifier-resource debt mixed with a
task-solving gap.
`protein-assembly` selected SR-014 because long work attempts reached Harbor
`AgentTimeoutError` with only partial mew reports instead of self-stopping from
a propagated wall budget.
`adaptive-rejection-sampler` selected SR-015 because the command-level timeout
was 1800s but Harbor's agent timeout still killed long trials at 900s; the
repair is to align the Batch 4 run shape with `--agent-timeout-multiplier 2`.
`cobol-modernization` matched the frozen Codex target cleanly.

Batch manifest:

`docs/M6_24_BATCH_4_MANIFEST_2026-04-29.md`

Batch JSON:

`docs/data/terminal_bench_m6_24_batch_4.json`

Frozen Codex Batch 4 target: **25/40 successes, 62.5%**.

## Results

| Task | Codex target | Mew result | Harbor errors | Runtime | Artifact |
|---|---:|---:|---:|---:|---|
| `sam-cell-seg` | 0/5 | 0/5 | 0 | 9m 26s | `proof-artifacts/terminal-bench/harbor-smoke/2026-04-29__02-16-06/result.json` |
| `train-fasttext`, partial only | 0/5 | 0/3 | 0 | >1h 22m, cancelled | `proof-artifacts/terminal-bench/harbor-smoke/2026-04-29__02-27-54/result.json` |
| `make-mips-interpreter` | 3/5 | 0/5 | 1 | 32m 17s | `proof-artifacts/terminal-bench/harbor-smoke/2026-04-29__03-53-16/result.json` |
| `torch-pipeline-parallelism` | 3/5 | 0/5 | 0 | 13m 19s | `proof-artifacts/terminal-bench/harbor-smoke/2026-04-29__04-42-38/result.json` |
| `protein-assembly` | 4/5 | 0/5 | 3 | 31m 56s | `proof-artifacts/terminal-bench/harbor-smoke/2026-04-29__04-59-28/result.json` |
| `adaptive-rejection-sampler` | 5/5 | 1/5 | 3 | 17m 45s | `proof-artifacts/terminal-bench/harbor-smoke/2026-04-29__05-46-31/result.json` |
| `cobol-modernization` | 5/5 | 5/5 | 0 | 4m 25s | `proof-artifacts/terminal-bench/harbor-smoke/2026-04-29__06-18-16/result.json` |
| `constraints-scheduling` | 5/5 | pending | pending | pending | pending |

Measured Batch 4 task total using latest full task runs: **6/30**, below the
frozen Codex target **20/30** for measured full tasks. The partial
`train-fasttext` run is excluded from the aggregate until a full run is
available or the campaign explicitly accepts the partial low-target control
evidence.

## `sam-cell-seg`

Artifact:

`proof-artifacts/terminal-bench/harbor-smoke/2026-04-29__02-16-06/result.json`

Result:

- `n_trials`: 5
- `n_errors`: 0
- `mean`: 0.000
- `pass@5`: 0.000
- reward `0.0`: `sam-cell-seg__NwFsE8c`,
  `sam-cell-seg__ZCs5Aws`, `sam-cell-seg__pPCxhv2`,
  `sam-cell-seg__xJfPF5n`, `sam-cell-seg__yDpt5Jw`
- runtime: 9m 26s

Failure classification:

- `failure_scope`: low-target control / task-solving
- signal:
  the run completed with no Harbor runner errors and matched the frozen Codex
  target of 0/5. Several trials produced scripts that only reached compile or
  help checks after local `cv2` / `numpy` dependency issues; one trial ran the
  external verifier much further and failed on mask alignment plus tuple-vs-list
  coordinate formatting.
- examples:
  `sam-cell-seg__pPCxhv2` ran the script and wrote `test_output.csv`, but the
  verifier failed `test_mask_alignment` and `test_coords_are_flat_lists`.
  Other trials failed to create `test_output.csv` after dependency/import
  problems.

Verdict: record as Batch 4 parity evidence and continue. Do not select a new
M6.14 repair solely from this task: it matched the Codex 0/5 target and did
not expose a new runner/substrate failure beyond ordinary task difficulty.

## `train-fasttext` Partial Attempt

Artifact:

`proof-artifacts/terminal-bench/harbor-smoke/2026-04-29__02-27-54/result.json`

Result:

- requested `n_total_trials`: 5
- completed `n_trials`: 3
- `n_errors`: 0 among completed trials
- `mean`: 0.000 among completed trials
- reward `0.0`: `train-fasttext__bh7YdwQ`,
  `train-fasttext__dmPFhQS`, `train-fasttext__u5tZAuY`
- cancelled before completion: `train-fasttext__5QTc3s8`,
  `train-fasttext__uy6CxPk`
- runtime before cancellation: >1h 22m

Failure classification:

- `failure_scope`: partial runner / benchmark-harness debt plus task-solving
- signal:
  the first three completed trials were 0/3, matching the frozen Codex 0/5
  low-target expectation. Several work reports show repeated 300s command
  timeouts while trying to install/train `fasttext` and build a model under
  the 150MB/0.62-accuracy constraint.
- runner evidence:
  two later trials were cancelled after Docker/Harbor teardown or verifier
  hangs. `trial.log` recorded `Docker compose down failed` with Docker daemon
  messages like `tried to kill container, but did not receive an exit event`.
  The top-level `result.json` remained unfinished with `finished_at: null`
  and `n_trials: 3`.

Verdict: do not count this as the final Batch 4 full task score. Also do not
select a core M6.14 repair solely from this low-target control: the mew work
loop produced useful timeout reports, and the blocking issue was external
Docker/Harbor cleanup behavior during a Codex-target-0 task. Continue Batch 4
with the next task instead of spending more wall time here.

## `make-mips-interpreter`

Artifact:

`proof-artifacts/terminal-bench/harbor-smoke/2026-04-29__03-53-16/result.json`

Result:

- `n_trials`: 5
- `n_errors`: 1
- `mean`: 0.000
- `pass@5`: 0.000
- reward `0.0`: `make-mips-interpreter__GZHpZHZ`,
  `make-mips-interpreter__PenyJY9`, `make-mips-interpreter__ZkoEzL5`,
  `make-mips-interpreter__wKphTT3`, `make-mips-interpreter__zUN94TS`
- exception `AgentTimeoutError`: `make-mips-interpreter__ZkoEzL5`
- runtime: 32m 17s

Failure classification:

- `failure_scope`: task-solving / surrogate execution fidelity
- signal:
  several trials reached `node vm.js`, produced `/tmp/frame.bmp`, and passed
  the frame existence/similarity checks, but failed the verifier's stdout check
  for `I_InitGraphics: DOOM screen size: w x h: 320 x 200`.
- examples:
  `make-mips-interpreter__PenyJY9`, `make-mips-interpreter__wKphTT3`, and
  `make-mips-interpreter__zUN94TS` created a frame similar to the reference but
  printed nearby Doom initialization text rather than the exact expected line.
  `make-mips-interpreter__GZHpZHZ` also produced a frame but lacked the exact
  stdout. `make-mips-interpreter__ZkoEzL5` timed out waiting for
  `/tmp/frame.bmp`.

Verdict: record as implementation-lane parity evidence, not an accepted M6.14
structural blocker. The exact `node vm.js` command ran and the verifier result
is visible; the remaining gap is high-fidelity MIPS/Doom behavior rather than
generic command execution, artifact observation, or finish-grounding substrate.

## `torch-pipeline-parallelism`

Artifact:

`proof-artifacts/terminal-bench/harbor-smoke/2026-04-29__04-42-38/result.json`

Result:

- `n_trials`: 5
- `n_errors`: 0
- `mean`: 0.000
- `pass@5`: 0.000
- reward `0.0`: `torch-pipeline-parallelism__AF7RKqg`,
  `torch-pipeline-parallelism__Ek8EArE`,
  `torch-pipeline-parallelism__xCRBurA`,
  `torch-pipeline-parallelism__yMobWdL`,
  `torch-pipeline-parallelism__igd835H`
- runtime: 13m 19s

Prior contaminated artifact:

- `proof-artifacts/terminal-bench/harbor-smoke/2026-04-29__04-27-57/result.json`
  produced 1/5, but the run happened before Docker cleanup and mixed successful
  verifier install with repeated `No space left on device` verifier failures.
  It is kept as diagnostic evidence, not the counted Batch 4 result.

Failure classification:

- `failure_scope`: verifier-resource debt plus task-solving
- resource signal:
  after reclaiming 5.627GB of stopped-container space, four verifier attempts
  still hit overlay/tmp exhaustion while installing or importing the heavy
  Torch/CUDA/Transformers stack. Examples include uv failing to extract
  `torch/lib/libtorch_cuda.so`, dpkg failing to write package metadata, and
  Python reporting no usable temp directory.
- task-solving signal:
  the one verifier that reached the functional tests failed gradient parity:
  `Rank 0 mismatch at model.layers.1.bwd, microbatch 0, max diff=0.013752...`.
  Agent-side local checks were mostly syntax/AST checks because `torch` was not
  available in the work container.

Verdict: count the completed Harbor result as 0/5 for the Batch 4 score, but do
not select a core M6.14 repair from this run. The dominant repeated blocker is
benchmark resource configuration for a very heavy verifier under parallel
execution; the generic mew work loop produced artifacts and reports. Continue
Batch 4 with `protein-assembly` instead of spending more wall time rerunning the
same resource-heavy task.

## `protein-assembly`

Artifact:

`proof-artifacts/terminal-bench/harbor-smoke/2026-04-29__04-59-28/result.json`

Result:

- `n_trials`: 5
- `n_errors`: 3
- exception `AgentTimeoutError`: `protein-assembly__LZRrgVa`,
  `protein-assembly__jtdRdvj`, `protein-assembly__aRd2BTz`
- `mean`: 0.000
- `pass@5`: 0.000
- reward `0.0`: `protein-assembly__LZRrgVa`,
  `protein-assembly__gjY8VUL`, `protein-assembly__jtdRdvj`,
  `protein-assembly__aRd2BTz`, `protein-assembly__GtzW6KW`
- runtime: 31m 56s

Failure classification:

- `failure_scope`: selected structural blocker plus task-solving gaps
- structural signal:
  three trials reached Harbor `AgentTimeoutError` with host-visible
  `mew-report.json` files still marked `partial_report`. Their effort state
  showed `wall_elapsed_seconds` around 1774-1798 seconds and active running
  model turns. Because the Harbor wrapper was delegating timeout to the outer
  task runner, the inner `mew work --oneshot` command had no
  `--max-wall-seconds` budget and could not self-stop with a final
  `wall_timeout` report before the outer agent timeout.
- task-solving signal:
  the two completed trials still failed the verifier on actual gBlock design
  quality: one produced no `gblock.txt`; another wrote non-DNA characters; and
  another translated to the wrong ordered fusion protein. These are not the
  selected repair target.

Verdict: pause broad M6.24 measurement and select SR-014 in
`docs/M6_14_STRUCTURAL_REPAIR_LEDGER.md`. The bounded generic repair is to let
the Harbor wrapper expose an explicit mew wall budget derived from the command
timeout, then run `mew work --oneshot` with `--max-wall-seconds` so long tasks
end with a final report instead of outer `AgentTimeoutError`.

SR-014 repair proof:

- `proof-artifacts/terminal-bench/harbor-smoke/mew-m6-14-sr014-protein-wall-budget/2026-04-29__05-38-06/result.json`
- one bounded `protein-assembly` same-shape trial with `timeout_seconds=120`
  and `{max_wall_seconds_option}` in the normal mew command template
- Harbor `n_errors=0`; transcript recorded `mew_max_wall_seconds=108`;
  final `mew-report.json` stopped as `wall_timeout` rather than
  `partial_report` or `AgentTimeoutError`

M6.24 can resume with `adaptive-rejection-sampler` using the updated Batch 4
manifest command shape.

## `adaptive-rejection-sampler`

Artifact:

`proof-artifacts/terminal-bench/harbor-smoke/2026-04-29__05-46-31/result.json`

Result:

- `n_trials`: 5
- `n_errors`: 3
- exception `AgentTimeoutError`: `adaptive-rejection-sampler__QZnZ25V`,
  `adaptive-rejection-sampler__Uhw7jP7`,
  `adaptive-rejection-sampler__dZZbA6F`
- `mean`: 0.200
- `pass@5`: 1.000
- reward `1.0`: `adaptive-rejection-sampler__rqKBhrk`
- reward `0.0`: `adaptive-rejection-sampler__JKDihSm`,
  `adaptive-rejection-sampler__QZnZ25V`,
  `adaptive-rejection-sampler__Uhw7jP7`,
  `adaptive-rejection-sampler__dZZbA6F`
- runtime: 17m 45s

Failure classification:

- `failure_scope`: selected structural blocker plus task-solving gaps
- structural signal:
  the run used wrapper `timeout_seconds=1800` and mew
  `--max-wall-seconds 1740`, but Harbor's own agent execution timeout remained
  900 seconds because the job did not set an agent timeout multiplier. Three
  trials therefore died with `AgentTimeoutError: Agent execution timed out
  after 900.0 seconds` before mew could reach its inner wall budget.
- task-solving signal:
  one trial passed the verifier and one non-exception trial failed the R
  sampler verifier. This task remains a real implementation challenge, but the
  selected repair target is timeout-budget alignment.

Verdict: select SR-015 in `docs/M6_14_STRUCTURAL_REPAIR_LEDGER.md`. The repair
is to include `--agent-timeout-multiplier 2` in the Batch 4 manifest whenever
the wrapper uses `timeout_seconds=1800` and the mew command uses
`{max_wall_seconds_option}`.

SR-015 repair proof:

- `proof-artifacts/terminal-bench/harbor-smoke/mew-m6-14-sr015-adaptive-agent-timeout-align/2026-04-29__06-06-11/result.json`
- one bounded same-shape `adaptive-rejection-sampler` trial with
  `--agent-timeout-multiplier 2`, wrapper `timeout_seconds=1800`, and
  `--max-wall-seconds 1740`
- Harbor `n_errors=0`; the trial reached a normal verifier result with reward
  0.0 instead of outer `AgentTimeoutError`

M6.24 can resume with `cobol-modernization` using the updated Batch 4 manifest
command shape.

## `cobol-modernization`

Artifact:

`proof-artifacts/terminal-bench/harbor-smoke/2026-04-29__06-18-16/result.json`

Result:

- `n_trials`: 5
- `n_errors`: 0
- `mean`: 1.000
- `pass@5`: 1.000
- reward `1.0`: `cobol-modernization__ZNecKuR`,
  `cobol-modernization__W9TnEtW`, `cobol-modernization__cbrsgSX`,
  `cobol-modernization__Sq64xVR`, `cobol-modernization__MCBLc87`
- runtime: 4m 25s

Failure classification:

- `failure_scope`: no failure

Verdict: clean pass. Count this as parity evidence and continue Batch 4 with
`constraints-scheduling`.

## Notes

- Do not add Terminal-Bench-specific solvers to core.
- Treat 0/5 frozen Codex targets as runner/permission controls unless mew
  exposes a generic bug or unexpected advantage.
- If a task reveals an accepted structural blocker, pause Batch 4, append it to
  `docs/M6_14_STRUCTURAL_REPAIR_LEDGER.md`, repair through M6.14, and rerun
  the same failed shape before continuing broad measurement.
