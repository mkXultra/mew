# M6.24 Decision Ledger

Purpose: keep the measure-vs-improve decisions for M6.24 in one compact,
append-friendly file so long sessions and context compression do not drift back
to stale next-batch execution.

Companion controller and data files:

- `docs/M6_24_GAP_IMPROVEMENT_LOOP.md`
- `proof-artifacts/m6_24_gap_ledger.jsonl`
- `docs/M6_24_GAP_BASELINE_2026-04-29.md`
- `docs/DESIGN_2026-04-26_RESIDENT_LANE_ARCHITECTURE.md`

## Controller Rule

After each batch or partial batch checkpoint, compare mew against the frozen
Codex target for the same measured trials.

Decision thresholds:

- `gap <= 10 percentage points`: classify any misses and continue broad
  measurement.
- `10 < gap <= 20 percentage points`: record gap classes and continue broad
  measurement unless the same class repeats across batches.
- `gap > 20 percentage points`: pause broad measurement and enter improvement
  phase.
- accepted structural blocker at any gap: pause M6.24, append/update
  `docs/M6_14_STRUCTURAL_REPAIR_LEDGER.md`, repair through M6.14, rerun the
  same failed shape, then resume M6.24.
- aggregate gap `> 20 percentage points` across three consecutive measured
  batches: enter improvement phase even if the latest batch alone is mixed.

Improvement phase requirements:

1. Choose exactly one generic gap class.
2. Name the failed task shape that will be rerun after repair.
3. Do not add Terminal-Bench-specific solvers.
4. Record before/after evidence.
5. Resume broad measurement only after the rerun is recorded or a written
   decision explains why the repair cannot be validated with the same shape.
6. Record classification and repair state in
   `proof-artifacts/m6_24_gap_ledger.jsonl`.
7. For any structural repair that changes task policy, lane behavior,
   helper-lane behavior, verifier authority, or repair loop shape, run the
   Architecture Fit Gate from `docs/M6_24_GAP_IMPROVEMENT_LOOP.md` before code
   changes start. Coding gaps default to implementation/tiny profile or guard
   changes unless the resident lane architecture explicitly justifies a new
   authoritative lane.

Allowed next actions during improvement phase:

- classify a measured failure into the gap ledger
- add missing instrumentation for a selected gap and speed-rerun the same shape
- make a bounded local/polish repair for a selected gap
- open/complete a reference-backed structural repair for a selected gap
- rerun the same failed shape after repair
- update this decision ledger to resume broad measurement with evidence

Anything else is drift unless the user's newest explicit instruction changes
direction.

## Decisions

| Date | Decision | Evidence | Next action | Status |
|---|---|---|---|---|
| 2026-04-29 | Enter M6.24 improvement phase; stop new broad measurement for now. | `docs/M6_24_GAP_BASELINE_2026-04-29.md`: measured subset is mew 92/210 = 43.8% vs Codex 156/210 = 74.3%, gap -30.5 pp; Batch 2, 3, 4, 5, and partial Batch 6 all exceed the `> 20 pp` improvement threshold. | Classify Batch 1-6 measured failures, pick one generic gap class, repair it, and rerun the same failed shape before continuing `gpt2-codegolf` or any new broad-measurement task. | active |
| 2026-04-29 | Select first gap class: `hard_task_implementation_strategy_contract_retention`. | `docs/M6_24_GAP_CLASS_PLAN_2026-04-29.md` and `proof-artifacts/m6_24_gap_ledger.jsonl`: many major losses remain after lower-level timeout, permission, artifact, and verifier-grounding repairs; repeated failures look like weak task decomposition, contract retention, source grounding, and verifier-driven repair on hard implementation tasks. | Produce a reference-backed design by inspecting mew evidence plus `references/fresh-cli/codex`, `docs/ADOPT_FROM_REFERENCES.md`, and `docs/REVIEW_2026-04-20_MISSING_PATTERNS_SURVEY.md`; implement the smallest generic work-session repair; rerun `make-doom-for-mips` or `make-mips-interpreter` same shape. | active |
| 2026-04-29 | Implement v0 hard-task contract capsule. | `docs/REVIEW_2026-04-29_M6_24_CODEX_IMPLEMENTATION_LANE_PATTERNS.md` and `docs/DESIGN_2026-04-29_M6_24_HARD_TASK_CONTRACT_CAPSULE.md`: Codex-style persistent objective/source grounding was translated into `working_memory.implementation_contract` plus a pre-finish source proof blocker. Focused validation passed for acceptance and work-session surfaces. | Rerun one same-shape hard task, preferably `make-doom-for-mips` or `make-mips-interpreter`, and record whether source grounding, surrogate/stub finishes, and reward improve. | rerun_recorded |
| 2026-04-29 | Same-shape rerun improved behavior but not reward. | `docs/M6_24_HARD_TASK_CONTRACT_RERUN_2026-04-29.md` and codex-ultra review `docs/REVIEW_2026-04-29_M6_24_HARD_CONTRACT_RERUN_NEXT.md`: `make-doom-for-mips` remained 0/5, but the failure shape changed from surrogate/stub completions to real source-build and VM-loader/runtime repair attempts; no false complete state was observed. Two trials still stopped on system package/read-write permission boundaries, and three reached real ELF/VM work before missing `/tmp/frame.bmp`. | Continue improvement phase. Do not run new broad measurement. Next bounded repair is generic hard-runtime verifier strategy: classify VM/emulator/interpreter failure signatures, preserve PC/opcode/artifact evidence, steer runtime-source plus readelf/nm/objdump/addr2line mapping, then rerun `make-doom-for-mips` same shape again. | active |
| 2026-04-29 | Implement v0 hard-runtime verifier strategy. | `docs/DESIGN_2026-04-29_M6_24_HARD_RUNTIME_VERIFIER_STRATEGY.md`: failed VM/emulator/interpreter verifier output now becomes `verifier_failure_repair_agenda.runtime_contract_gap` with kind, PC/opcode/artifact signature, recommended mapping tools, and resume-visible next action. Reasoning policy also promotes MIPS/ELF/toolchain/provided-source/VM-style implementation tasks to high effort. Focused work-session and reasoning-policy validation passed. | Rerun `make-doom-for-mips` same shape and record whether the runtime gap appears in reentry, permission waits are reduced/absent, and at least one trial passes the current VM startup/opcode blocker or writes `/tmp/frame.bmp`. | rerun_pending |
| 2026-04-29 | Hard-runtime rerun improved external verifier proximity but not reward. | `docs/M6_24_HARD_RUNTIME_RERUN_2026-04-29.md`: `make-doom-for-mips` remained 0/5, but permission waits disappeared, no surrogate/stub completion dominated, all trials pursued real source/runtime routes, and one trial self-verified exact `node vm.js` plus a valid 640x400 32bpp `/tmp/frame.bmp`. External verifier reached 2/3 on the best trial but failed stdout timing because stale `/tmp/frame.bmp` from self-verification caused the verifier to terminate the fresh VM process too early. | Continue improvement phase. Do not run new broad measurement. Next bounded repair is generic runtime artifact freshness / external verifier alignment: preserve self-check evidence but clean stale runtime artifacts before finish when the verifier is expected to create them, surface `stale_runtime_artifact_risk`, then rerun `make-doom-for-mips` same shape again. | active |
| 2026-04-29 | Implement v0 runtime artifact freshness repair. | `docs/DESIGN_2026-04-29_M6_24_RUNTIME_ARTIFACT_FRESHNESS.md`: `acceptance_finish_blocker()` now blocks `task_done=true` when a fresh-runtime task self-verifies a generated `/tmp/...` artifact but no later cleanup is visible. Work-session resume surfaces `stale_runtime_artifact_risk`, and THINK guidance tells the model to preserve proof then clean stale runtime artifacts before finish unless the task requires the artifact to pre-exist. Focused acceptance/work-session validation and ruff passed. | Rerun `make-doom-for-mips` same shape and record whether the stale `/tmp/frame.bmp` stdout-timing failure disappears or reward improves. | rerun_pending |
| 2026-04-29 | Add Resident Lane Architecture fit gate to structural gap repairs. | The hard-task repair sequence showed that `hard task` was treated as a failure symptom without an explicit lane/profile/helper decision. `docs/M6_24_GAP_IMPROVEMENT_LOOP.md` now makes `docs/DESIGN_2026-04-26_RESIDENT_LANE_ARCHITECTURE.md` authoritative for structural design fit and requires an Architecture Fit Gate before code changes that affect lane behavior or repair loop shape. | Future M6.24 structural designs must record whether the repair is `no_lane_change`, `implementation_profile`, `helper_lane`, or `new_lane`; coding gaps default to implementation/tiny profile or guard changes unless a new authoritative lane is explicitly justified. | active |
| 2026-04-29 | Runtime artifact freshness rerun removed the stale-artifact short-circuit but reward stayed 0/5. | `docs/M6_24_RUNTIME_FRESHNESS_RERUN_2026-04-29.md` and `proof-artifacts/terminal-bench/harbor-smoke/mew-m6-24-runtime-freshness-make-doom-5attempts-20260429-1425/result.json`: five trials, no runner errors, all rewards 0.0. External verifiers no longer failed because a stale `/tmp/frame.bmp` existed too early; they waited for a fresh frame and failed because no final `/tmp/frame.bmp` existed. | Continue improvement phase. Do not resume broad measurement. Before another hard-task structural repair, record `hard_task profile v0`, then repair `hard_runtime_final_verifier_state_transfer` and rerun `make-doom-for-mips` same shape. | active |
| 2026-04-29 | Record `hard_task profile v0` as an implementation-lane profile, not a new lane. | `docs/DESIGN_2026-04-29_M6_24_HARD_TASK_PROFILE_V0.md`: hard-task detection, policy, reentry fields, finish gates, helper-lane boundary, and architecture decision are now centralized. The profile keeps hard coding tasks in `implementation/tiny` and names `hard_runtime_final_verifier_state_transfer` as the immediate next repair boundary. | Implement the smallest generic final-verifier state-transfer repair, then rerun `make-doom-for-mips` same shape. | active |
| 2026-04-29 | Adopt speed-rerun-first budget for M6.24 repairs. | Repeated `-k 5 -n 5` repair reruns are too expensive for diagnosis. `docs/M6_24_GAP_IMPROVEMENT_LOOP.md` now treats `-k 5 -n 5` as close/escalation proof, not the default repair loop. | Next repair should use a 1-trial same-shape speed rerun first. Escalate to 5 trials only if the speed rerun shows material improvement, contradictory variance, or the ledger is ready to resume broad measurement. | active |
| 2026-04-29 | Implement v0 final-verifier state transfer guard. | `docs/DESIGN_2026-04-29_M6_24_FINAL_VERIFIER_STATE_TRANSFER.md`: finish now blocks fresh-runtime tasks that cite command success without proving the required `/tmp/...` artifact creation; resume surfaces `final_verifier_state_transfer`; THINK guidance warns that hard runtime/VM command exit 0 is not final verifier state transfer. Focused acceptance/work-session tests and ruff passed. | Run a 1-trial same-shape speed-rerun for `make-doom-for-mips`; do not run `-k 5 -n 5` unless the speed proof gives a reason to escalate. | speed_rerun_pending |
| 2026-04-29 | Final-verifier state transfer speed-rerun was directionally improved but score stayed 0/1. | `docs/M6_24_FINAL_VERIFIER_TRANSFER_SPEED_RERUN_2026-04-29.md` and `proof-artifacts/terminal-bench/harbor-smoke/mew-m6-24-final-verifier-transfer-make-doom-1attempt-20260429-1518/result.json`: one trial, no runner errors, reward 0.0. Resume surfaced `final_verifier_state_transfer`; the model did not finish after `node vm.js` exit 0 without artifact proof, proved `/tmp/frame.bmp` was missing, and advanced to `W_GetNumForName: STCFN33 not found`. | Do not escalate to 5 trials. Decide the next highest-leverage M6.24 gap: either sample a different hard task with 1 trial to check profile generality, or select a new generic repair only if repeated evidence shows a concrete next-source-repair timeout pattern. | active |
| 2026-04-29 | Hard-task profile generalized to `make-mips-interpreter`, but discovered stale artifact freshness blocked reward. | `docs/M6_24_HARD_PROFILE_MAKE_MIPS_SPEED_RERUN_2026-04-29.md` and `proof-artifacts/terminal-bench/harbor-smoke/mew-m6-24-hard-profile-make-mips-interpreter-1attempt-20260429-1555/result.json`: one trial, no runner errors, reward 0.0. mew implemented `vm.js`, exact `node vm.js` booted Doom, and the external verifier passed frame existence plus image similarity. It failed only stdout because mew left the discovered `/tmp/frame.bmp` from self-verification, causing the fresh verifier process to be terminated before the expected `I_InitGraphics` line. | v0.1 generalized stale-runtime-artifact detection to `/tmp/...` paths discovered in verified checks or tool output when task text says a fresh runtime writes generated frames/artifacts. Run a 1-trial same-shape speed-rerun for `make-mips-interpreter`; do not escalate to 5 trials unless it passes or materially improves. | speed_rerun_pending |
| 2026-04-29 | v0.1 same-shape speed rerun still failed because cleanup had no final turn. | `docs/M6_24_DISCOVERED_ARTIFACT_CLEANUP_RERUN_2026-04-29.md` and `proof-artifacts/terminal-bench/harbor-smoke/mew-m6-24-discovered-artifact-make-mips-interpreter-1attempt-20260429-1620/result.json`: one trial, no runner errors, reward 0.0. The final successful tool #30 ran exact `node vm.js`, emitted expected `I_InitGraphics` stdout, saved `/tmp/frame.bmp`, and exited 0. The session then hit `wall_timeout` before finish/cleanup, so the stale frame remained and the external verifier failed the same stdout timing check. | Implement `--defer-verify` post-run cleanup for resume-visible `/tmp/...` stale runtime artifacts before external verifier handoff. Keep this generic to one-shot handoff, not Terminal-Bench. | active |
| 2026-04-29 | Implement v0.2 deferred-verifier runtime artifact cleanup. | `docs/DESIGN_2026-04-29_M6_24_RUNTIME_ARTIFACT_FRESHNESS.md` and `src/mew/commands.py`: `mew work --oneshot --defer-verify` now removes `/tmp/...` artifacts surfaced by `stale_runtime_artifact_risk` before returning to the external harness, records `post_run_cleanup`, and clears the stale risk when all cleanup succeeds. Marker coverage now recognizes `saved frame` output. Focused acceptance/work-session validation and ruff passed. | Run a 1-trial same-shape speed-rerun for `make-mips-interpreter`; do not escalate to `-k 5 -n 5` unless it passes or materially improves. | speed_rerun_pending |
| 2026-04-29 | v0.2 same-shape speed rerun still failed because the final resume missed the stale artifact risk. | `docs/M6_24_DEFER_VERIFY_CLEANUP_SPEED_RERUN_2026-04-29.md` and `proof-artifacts/terminal-bench/harbor-smoke/mew-m6-24-defer-verify-cleanup-make-mips-interpreter-1attempt-20260429-1702/result.json`: one trial, no runner errors, reward 0.0. mew finished normally after exact `node vm.js` succeeded, Doom booted, and `/tmp/frame.bmp` plus `/tmp/frame_000001.bmp` were validated. `post_run_cleanup` was `{}` because resume risk was empty, so the stale frame survived and the external verifier failed the same stdout timing check. | Implement report-step fallback cleanup: if resume risk is empty, build stale runtime artifact risk from `work_report.steps[*].tool_call`. Also recognize `saved /tmp/` and `exists size=` output markers. | active |
| 2026-04-29 | Implement v0.3 report-step fallback for deferred verifier cleanup. | `docs/DESIGN_2026-04-29_M6_24_RUNTIME_ARTIFACT_FRESHNESS.md` and `src/mew/commands.py`: one-shot cleanup now falls back to completed tool calls in `work_report.steps` when resume lacks stale artifact risk. Focused acceptance/work-session validation and ruff passed. Open GitHub issues #25-#27 were checked and remain M6.16-style implementation-lane hardening inputs, not blockers for this M6.24 repair. | Run another 1-trial same-shape speed-rerun for `make-mips-interpreter`; do not escalate to `-k 5 -n 5` unless it passes or materially improves. | speed_rerun_pending |
| 2026-04-29 | v0.3 same-shape speed rerun passed. | `docs/M6_24_DEFER_VERIFY_CLEANUP_SPEED_RERUN_2026-04-29.md` and `proof-artifacts/terminal-bench/harbor-smoke/mew-m6-24-report-step-cleanup-make-mips-interpreter-1attempt-20260429-1727/result.json`: one trial, no runner errors, reward 1.0. `post_run_cleanup.kind` was `deferred_verify_runtime_artifact_cleanup`, `/tmp/frame-000001.bmp` was removed before handoff, and the external verifier passed VM execution, frame existence, and frame similarity. | Escalate to a five-trial same-shape proof for `make-mips-interpreter`. If it reaches the frozen Codex target `3/5` without a new repeated structural blocker, record the repair as stable enough to choose the next gap or resume broader measurement. | proof_5_pending |
| 2026-04-29 | Five-trial proof improved but stayed below Codex target. | `docs/M6_24_MAKE_MIPS_PROOF_5_2026-04-29.md` and `proof-artifacts/terminal-bench/harbor-smoke/mew-m6-24-report-step-cleanup-make-mips-interpreter-5attempts-20260429-1802/result.json`: `make-mips-interpreter` scored 1/5 with errors 0 against frozen Codex target 3/5. Repeated failures now include 320x200 frame artifacts accepted by self-checks where the external verifier expected 640x400, plus one stdout timing miss and one model JSON parse failure. | Continue improvement phase. Next bounded repair is `runtime_visual_artifact_quality_contract`: require grounded quality evidence for rendered-frame/runtime visual artifact tasks, then rerun `make-mips-interpreter` with a 1-trial speed proof. | active |
| 2026-04-29 | Implement v0.4 runtime visual artifact quality contract. | `docs/DESIGN_2026-04-29_M6_24_RUNTIME_ARTIFACT_FRESHNESS.md`: runtime frame/screenshot/image-output tasks with correct/expected/check language now require grounded evidence for expected dimensions/resolution, reference similarity, or exact stdout/boot markers. `acceptance_finish_blocker()` blocks finish when evidence only proves artifact existence, valid headers, nonzero pixels, or self-consistent dimensions. Focused acceptance/prompt tests, ruff, `git diff --check`, and gap-ledger JSON validation passed. | Run a 1-trial same-shape speed proof for `make-mips-interpreter`; do not resume broad measurement before recording that rerun. | speed_rerun_pending |
| 2026-04-29 | v0.4 same-shape speed rerun passed. | `docs/M6_24_VISUAL_QUALITY_SPEED_RERUN_2026-04-29.md` and `proof-artifacts/terminal-bench/harbor-smoke/mew-m6-24-visual-quality-make-mips-interpreter-1attempt-20260429-1845/result.json`: one trial, no runner errors, reward 1.0. mew verified Doom boot markers and fresh `640x400` frame BMPs before finish, cleaned `/tmp` frame artifacts before handoff, and the external verifier passed VM execution, frame existence, and frame similarity. | Escalate to a five-trial same-shape proof for `make-mips-interpreter`. If it reaches the frozen Codex target `3/5` without a new repeated structural blocker, record this repair as stable enough to choose the next gap or resume broad measurement. | proof_5_pending |
| 2026-04-29 | v0.4 five-trial proof stayed below target and selected verifier-handoff repair. | `docs/M6_24_VISUAL_QUALITY_PROOF_5_2026-04-29.md` and `proof-artifacts/terminal-bench/harbor-smoke/mew-m6-24-visual-quality-make-mips-interpreter-5attempts-20260429-1908/result.json`: `make-mips-interpreter` scored `1/5` with errors 0 against frozen Codex target `3/5`. One trial passed; one finished with valid `/tmp/frame.bmp` evidence but missed deferred cleanup because validator output was not recognized; one finished with valid frame evidence at `frames/frame000001.bmp` / root copy while the external verifier expected `/tmp/frame.bmp`; two timed out in real VM/runtime repair paths. | Continue improvement phase. Next bounded repair is `runtime_external_artifact_path_and_cleanup_contract`: require exact verifier-read artifact path evidence and extend deferred cleanup detection for `/tmp/...` paths mentioned by validation output. Then run a 1-trial same-shape speed proof for `make-mips-interpreter`. | active |
| 2026-04-29 | Implement v0.5 external artifact path and cleanup contract. | `docs/DESIGN_2026-04-29_M6_24_RUNTIME_ARTIFACT_FRESHNESS.md`: artifact discovery now recognizes expected verifier `/tmp/...` paths from runtime output, cleanup recognizes validator output such as `BMP ok: ... and /tmp/...`, finish blocks sibling-path proof when the verifier-read `/tmp/...` path is not cited, and THINK guidance warns against finishing after proving only `frames/...`, `output/...`, or root copies. Focused acceptance/work-session tests and ruff passed. | Run a 1-trial same-shape speed proof for `make-mips-interpreter`; do not resume broad measurement before recording that rerun. | speed_rerun_pending |
| 2026-04-29 | v0.5 same-shape speed rerun passed. | `docs/M6_24_ARTIFACT_HANDOFF_SPEED_RERUN_2026-04-29.md` and `proof-artifacts/terminal-bench/harbor-smoke/mew-m6-24-artifact-handoff-make-mips-interpreter-1attempt-20260429-1950/result.json`: one trial, no runner errors, reward 1.0. mew did not accept a wrong-path finish; it stayed in the hard runtime repair loop until `max_steps`, applied a final MIPS SPECIAL3 `EXT` / `INS` repair, and the external verifier passed VM execution, frame existence, and frame similarity. The cleanup-marker path was not directly exercised because no final self-verification artifact was created before handoff. | Escalate to a five-trial same-shape proof for `make-mips-interpreter`. If it reaches the frozen Codex target `3/5` without a new repeated structural blocker, record this repair stable enough to choose the next gap or resume broader measurement. | proof_5_pending |
| 2026-04-29 | v0.5 five-trial proof reached the frozen Codex target. | `docs/M6_24_ARTIFACT_HANDOFF_PROOF_5_2026-04-29.md` and `proof-artifacts/terminal-bench/harbor-smoke/mew-m6-24-artifact-handoff-make-mips-interpreter-5attempts-20260429-2021/result.json`: `make-mips-interpreter` scored `3/5` with runner errors 0, matching the frozen Codex target `3/5`. The previous wrong-path and stale-cleanup handoff misses did not recur. Remaining failures were VM stdout capture and image similarity quality, not the selected artifact-handoff blocker. | Close the selected artifact-handoff repair, then re-evaluate the aggregate gap before broad measurement resumes. | repair_closed_threshold_recheck_required |
| 2026-04-29 | Correct measurement-resume drift after threshold recheck. | `docs/M6_24_GAP_BASELINE_2026-04-29.md` still shows aggregate gap `-30.5pp`, and `docs/M6_24_BATCH_6_RUNS_2026-04-29.md` now records `gpt2-codegolf` at `0/5` vs Codex target `5/5`, leaving Batch 6 at `10/19` vs `20/20`. The selected repair closed one gap, but it did not bring aggregate or current-batch gap under the `>20pp` improvement threshold. | Stay in improvement phase. Do not run `headless-terminal` or other new broad tasks. Classify/select the next highest-leverage gap class, with `gpt2-codegolf` as fresh evidence for compact model/inference implementation contract failure. | active |
| 2026-04-29 | Select next gap class: `compact_model_inference_contract_failure`. | `docs/M6_24_BATCH_6_RUNS_2026-04-29.md` and `proof-artifacts/terminal-bench/harbor-smoke/mew-m6-24-batch6-gpt2-codegolf-5attempts-20260429-2103/result.json`: `gpt2-codegolf` scored `0/5` with runner errors 0 while all trials finished after compile/size/token-count smoke evidence. External verifier failures show wrong or degenerate model continuation, so the gap is semantic model-output contract retention, not setup debt. Architecture fit is `implementation_profile` in the existing implementation/tiny lane. | Implement `docs/DESIGN_2026-04-29_M6_24_MODEL_INFERENCE_OUTPUT_CONTRACT.md`: a generic model/checkpoint/tokenizer inference output finish guard plus prompt guidance, then run a one-trial same-shape speed proof for `gpt2-codegolf`. | active |
| 2026-04-29 | Implement v0 model-inference output contract guard. | `docs/M6_24_MODEL_INFERENCE_CONTRACT_IMPLEMENTATION_2026-04-29.md`: finish now blocks model/checkpoint/tokenizer inference tasks that cite only compile, size, CLI shape, source-read, or token-count smoke evidence. Completion requires tool-result-grounded reference/golden/oracle, expected-continuation, argmax/top-1, logits, token-id, or same-token equivalence. Focused tests, ruff, `git diff --check`, and codex-ultra review passed. | Run one-trial same-shape speed proof for `gpt2-codegolf`; do not run `headless-terminal` or resume broad measurement first. | speed_rerun_pending |
| 2026-04-29 | v0 model-inference speed rerun improved behavior but score stayed `0/1`. | `docs/M6_24_MODEL_INFERENCE_SPEED_RERUN_2026-04-29.md` and `proof-artifacts/terminal-bench/harbor-smoke/mew-m6-24-model-inference-gpt2-codegolf-1attempt-20260429-2221/2026-04-29__22-21-46/result.json`: one trial, no runner errors, reward 0.0. mew produced a real 3124-byte checkpoint/BPE-reading C implementation, but finished with `task_done=false` while explicitly saying exact GPT-2 equivalence was unverified; external verifier failed exact continuation with `WARRANT8` vs `WARRANTY`. | Do not escalate to five trials. Implement `model_inference_incomplete_handoff_guard`: for model/checkpoint/tokenizer inference tasks, apply the model-output acceptance gate to finish handoffs even when `task_done=false`, then run another one-trial same-shape speed proof. | active |
| 2026-04-29 | Implement v0.1 model-inference incomplete handoff guard. | `docs/M6_24_MODEL_INFERENCE_CONTRACT_IMPLEMENTATION_2026-04-29.md`: `finish` with `task_done=false` no longer bypasses the model-output quality gate for model/checkpoint/tokenizer inference tasks. Grounded equivalence can still hand off; unverified exact output blocks finish and continues the repair loop. Focused tests and ruff passed. | Run one-trial same-shape speed proof for `gpt2-codegolf`; do not run `headless-terminal` or resume broad measurement first. | speed_rerun_pending |
| 2026-04-29 | v0.1 same-shape speed rerun blocked incomplete handoff but score stayed `0/1`. | `docs/M6_24_MODEL_INFERENCE_HANDOFF_GUARD_RERUN_2026-04-29.md` and `proof-artifacts/terminal-bench/harbor-smoke/mew-m6-24-incomplete-handoff-gpt2-codegolf-1attempt-20260429-2250/2026-04-29__22-50-18/result.json`: one trial, no runner errors, reward 0.0. The session no longer closed via unverified finish; it used 30 steps, stopped with `model_error/request timed out`, and external verifier tested the remaining implementation. All 30 model turns used `reasoning_effort=medium`. | Do not escalate to five trials. Implement `model_inference_complex_reasoning_policy`: classify checkpoint/tokenizer/model-inference implementation tasks as `complex_implementation` so they use high reasoning effort and full prompt context, then run another one-trial same-shape speed proof. | active |
| 2026-04-29 | Implement v0.2 model-inference complex reasoning policy. | `docs/M6_24_MODEL_INFERENCE_CONTRACT_IMPLEMENTATION_2026-04-29.md`: checkpoint/tokenizer/model-inference terms such as `.ckpt`, `.bpe`, `vocab.bpe`, `model weights`, `tokenizer`, `transformer`, and `model inference` now select `complex_implementation`/high effort for implementation-capable work turns. Focused reasoning-policy/work-session tests and ruff passed. | Run one-trial same-shape speed proof for `gpt2-codegolf`; do not run `headless-terminal` or resume broad measurement first. | speed_rerun_pending |
| 2026-04-29 | v0.2 high-effort rerun confirmed policy but score stayed `0/1`. | `docs/M6_24_MODEL_INFERENCE_HIGH_EFFORT_RERUN_2026-04-29.md` and `proof-artifacts/terminal-bench/harbor-smoke/mew-m6-24-high-effort-gpt2-codegolf-1attempt-20260429-2335/2026-04-29__23-35-53/result.json`: one trial, no runner errors, reward 0.0. All 22 model turns used high effort, but mew repeatedly tried to finish using a candidate-derived "standard-libm reference" / `candidate_equals_reference` check and then hit `wall_timeout`; external verifier still failed exact continuation with repeated `Damien` tokens. | Do not escalate to five trials. Implement `model_inference_oracle_provenance_guard`: reject self-derived candidate/reference oracle evidence and give a specific repair signal, then run another one-trial same-shape speed proof. | active |
| 2026-04-29 | Implement v0.3 model-inference oracle provenance guard. | `docs/M6_24_MODEL_INFERENCE_CONTRACT_IMPLEMENTATION_2026-04-29.md`: model-output evidence can recognize `candidate_equals_reference` / `expected_continuation` formats, but rejects references/oracles built from the current candidate implementation or same source. Focused model-inference acceptance tests and ruff passed. | Run one-trial same-shape speed proof for `gpt2-codegolf`; do not run `headless-terminal` or resume broad measurement first. | speed_rerun_pending |

## Current Mode

`improvement_phase`

The selected `runtime_external_artifact_path_and_cleanup_contract` repair has a
same-shape five-trial proof at the frozen Codex target, so that repair is
closed. The aggregate and current Batch 6 gaps remain above the M6.24
improvement threshold, so broad measurement must stay paused.

Current selected next action:

`M6.24 -> compact_model_inference_contract_failure -> model_inference_oracle_provenance_guard -> speed_1 same-shape rerun for gpt2-codegolf`
