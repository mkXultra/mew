# M6.24 Gap Recheck - 2026-04-30

Purpose: reclassify the remaining M6.24 gap after the two Batch 6 repair proofs
that reached frozen Codex targets:

- `gpt2-codegolf`: `5/5` after generated-oracle provenance guard
- `git-multibranch`: `5/5` after system-service ACT capability-boundary repair

## Current State

Operational adjusted aggregate:

- original baseline: roughly `92/210` mew vs `156/210` Codex
- after `gpt2-codegolf` proof: roughly `97/210`
- after `git-multibranch` proof: roughly `101/210`
- adjusted gap: `101/210 = 48.1%` vs `156/210 = 74.3%`, about `-26.2pp`

Batch 6 is no longer the best next repair source:

- adjusted measured Batch 6 slice is `19/20` vs Codex `20/20`
- the remaining aggregate gap is dominated by earlier Batch 1-5 failures

Therefore M6.24 stays in `improvement_phase`, but the next selected gap should
not come from the next broad Batch 6 task.

## Selected Next Gap Class

Selected gap:

`long_dependency_toolchain_build_strategy_contract`

Architecture fit:

- `architecture_decision`: `implementation_profile`
- `authoritative_lane`: `tiny`
- no new lane in M6.24
- likely repair surface: implementation/tiny planning, build-state handoff,
  dependency/toolchain strategy, timeout-aware progress proof

Evidence:

- `compile-compcert`: `0/5` vs Codex target `5/5`
  - original failure was immediate permission wait for `/tmp/CompCert`
  - SR-016 fixed generic `/tmp` scratch permission
  - same-shape SR-016 proof reached `/tmp/CompCert` work and ended with
    `/tmp/CompCert/ccomp` missing
- `mcmc-sampling-stan`: `0/5` vs Codex target `2/5`
  - shell-chain substrate repaired, remaining failure centered on missing
    `rstan`, missing output files, or fallback analysis after timeout
- `protein-assembly`: `0/5` vs Codex target `4/5`
  - wall-budget substrate repaired, remaining evidence is partial long-domain
    progress under large dependency/model work
- `adaptive-rejection-sampler`: `1/5` vs Codex target `5/5`
  - Harbor timeout alignment repaired, remaining gap is task-solving /
    long-running statistical implementation strategy

## Why This Is Next

This class is larger than local precision fixes such as
`count-dataset-tokens` or `custom-memory-heap-crash`, and it is better grounded
than adding another visual/document observation repair right now.

It also fits the M6.24 goal: reduce the largest remaining generic gap before
spending more broad measurement budget.

## Next Action

Do not implement a new repair yet.

First run a one-trial same-shape speed rerun for `compile-compcert` on current
head with the current generic M6.24 wrapper:

```text
M6.24 -> long_dependency_toolchain_build_strategy_contract -> implementation_profile/no_lane_change -> selected-gap disambiguation speed rerun -> compile-compcert speed_1
```

Reason:

- the original five-trial result was dominated by a permission blocker that is
  now repaired;
- the one-trial SR-016 proof was intentionally bounded and showed the shape had
  moved, but did not classify the remaining strategy gap enough to design the
  next repair;
- one current-head speed rerun is the cheapest way to distinguish:
  - already-improved behavior that may warrant proof/escalation,
  - a repeated long-build timeout strategy gap,
  - or a new concrete structural blocker.

Only after that rerun should the resident decide whether to implement a
reference-backed repair, add instrumentation, or pick a different remaining
gap class.
