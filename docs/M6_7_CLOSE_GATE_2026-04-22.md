# M6.7 Close Gate 2026-04-22

Status: close-gate proof passed.

This document closes Milestone 6.7, Supervised Self-Hosting Loop. The claim is
narrow: mew can now implement bounded roadmap work under reviewer gating,
preserve proof-or-revert discipline, survive a real context reload, and hold
that supervised shape across a multi-hour proof window without hidden reviewer
rescue edits.

## Behavior Proved

M6.7 closes on four concrete behaviors from `ROADMAP.md`:

- mew can run one bounded roadmap iteration end to end, stop for review, and
  attach a usable proof artifact without hidden reviewer rescue edits
- proof-or-revert is enforced: missing or failing proof does not earn credit
- scope fence prevents out-of-scope edits and blocks self-authored
  roadmap-status or milestone-close changes without human approval
- a supervised run can span `>=4h` wall-clock, complete `>=3` real roadmap
  items, include `>=1` real reentry/pause-resume across a context reload, and
  keep drift canary green with zero proof-or-revert failures

## Final Close-Gate Proof

Run shape:

- supervised run start: `2026-04-22 01:29 JST`
- frozen close-watch copy created: `2026-04-22 02:31 JST`
- frozen close-watch path: `/private/tmp/mew-m67-close-watch-20260422-023401`
- frozen close-watch head: `b89f35a`
- earliest close eligibility: `2026-04-22 05:29 JST`

The user explicitly approved the split execution mode: freeze the active M6.7
proof window in a detached copy once all gate conditions except elapsed
wall-clock time were already satisfied, then allow bounded M6.9 Phase 1 work
to continue on mainline while the frozen copy preserved the M6.7 proof state.

This close relies on the frozen copy only for the remaining wall-clock
criterion. All other close-gate evidence was already recorded before the split.

## Evidence Used

Real supervised-proof credit:

- `docs/M6_7_FIRST_SUPERVISED_ITERATION_2026-04-21.md`
- `docs/M6_7_SECOND_SUPERVISED_ITERATION_2026-04-21.md`
- `docs/M6_7_SIXTH_SUPERVISED_ITERATION_2026-04-21.md`
- `docs/M6_7_SUPERVISED_8H_PROOF_PLAN_2026-04-21.md` candidate outcomes for:
  - `N-G` via task `#386` / session `#375`
  - `N-I` via task `#387` / session `#376`
  - `N-J` via task `#389` / session `#380`

Real reentry evidence:

- task `#388` / session `#378`
  - resumed the same supervised run after interruption/context reload
  - preserved exact blocker outcomes without becoming a proof-or-revert failure

Reviewer-owned gate state:

- `ROADMAP_STATUS.md` recorded before freeze that:
  - `>=3` real roadmap items were already satisfied
  - `>=1` real reentry was already satisfied
  - reviewer decisions were recorded on each credited iteration
  - proof-or-revert failures remained zero for the credited run
  - drift canary stayed green throughout the credited run
  - only `>=4h` wall-clock remained

## Gate Checklist

- bounded roadmap iteration with reviewer-visible diff and no hidden rescue:
  `pass`
- proof-or-revert enforced:
  `pass`
- scope fence blocks self-authored roadmap/milestone edits:
  `pass`
- `>=3` real roadmap items:
  `pass` (`N-G`, `N-I`, `N-J`)
- `>=1` real reentry / pause-resume across context reload:
  `pass` (`#388` / session `#378`)
- reviewer decisions recorded on each credited iteration:
  `pass`
- zero proof-or-revert failures in the credited run:
  `pass`
- green drift canary throughout:
  `pass`
- `>=4h` wall-clock under the same supervised run:
  `pass` (from `2026-04-22 01:29 JST` to `2026-04-22 05:29 JST`)

## Closure Caveat

The final `>=4h` condition was satisfied by preserving the supervised proof
window in a detached frozen copy while mainline moved on to bounded M6.9 Phase
1 work. That means the last condition is a retained-state proof, not a live
foreground loop that kept consuming new M6.7 items during the entire window.

This does not invent new evidence. It isolates the already-satisfied
supervised-loop state so the remaining elapsed-time criterion can accrue
honestly without polluting the M6.7 proof window with unrelated M6.9 code.

## Interpretation

M6.7 is done because mew now has a reviewer-audited self-hosting loop, not
merely isolated mew-first wins:

- mew can implement bounded roadmap tasks under reviewer gating
- proof-or-revert and scope fence are enforced as product behavior, not policy
  intent
- a real interrupted run can be resumed without losing the supervised shape
- the loop can stay valid across a multi-hour proof window

Future work can reduce reviewer dispatch latency (M6.8) and turn durable state
into coding advantage (M6.9), but M6.7 no longer blocks the roadmap.
