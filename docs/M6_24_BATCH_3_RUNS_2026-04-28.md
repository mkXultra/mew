# M6.24 Batch 3 Runs

Date: 2026-04-28 JST

Status: in progress; `break-filter-js-from-html` measured with SR-008 repaired;
`build-pov-ray` measured with SR-004 repaired; `polyglot-rust-c` measured with
SR-010 repaired; `model-extraction-relu-logits` measured with SR-011 repaired.

Batch manifest:

`docs/M6_24_BATCH_3_MANIFEST_2026-04-28.md`

Batch JSON:

`docs/data/terminal_bench_m6_24_batch_3.json`

Frozen Codex Batch 3 target: **24/40 successes, 60.0%**.

## Results

| Task | Codex target | Mew result | Harbor errors | Runtime | Artifact |
|---|---:|---:|---:|---:|---|
| `install-windows-3.11` | 0/5 | pending | pending | pending | pending |
| `make-doom-for-mips` | 1/5 | pending | pending | pending | pending |
| `mcmc-sampling-stan` | 2/5 | pending | pending | pending | pending |
| `video-processing` | 3/5 | pending | pending | pending | pending |
| `model-extraction-relu-logits` | 4/5 | 0/5 | 0 | 15m 7s | `proof-artifacts/terminal-bench/harbor-smoke/2026-04-28__22-02-51/result.json` |
| `polyglot-rust-c` | 4/5 | 0/5 | 0 | 11m 23s | `proof-artifacts/terminal-bench/harbor-smoke/2026-04-28__20-45-24/result.json` |
| `break-filter-js-from-html` | 5/5 | 0/5 | 0 | 4m 6s | `proof-artifacts/terminal-bench/harbor-smoke/2026-04-28__19-47-14/result.json` |
| `break-filter-js-from-html` SR-008 repair proof, 1 trial | 5/5 | 0/1 | 0 | 8m 18s | `proof-artifacts/terminal-bench/harbor-smoke/2026-04-28__19-55-56/result.json` |
| `build-pov-ray` | 5/5 | 2/5 | 0 | 11m 58s | `proof-artifacts/terminal-bench/harbor-smoke/2026-04-28__20-16-05/result.json` |
| `build-pov-ray` SR-004 repair proof, 1 trial | 5/5 | 1/1 | 0 | 9m 6s | `proof-artifacts/terminal-bench/harbor-smoke/2026-04-28__20-33-30/result.json` |
| `polyglot-rust-c` SR-010 repair proof, 1 trial | 4/5 | 0/1 | 0 | 7m 16s | `proof-artifacts/terminal-bench/harbor-smoke/2026-04-28__21-23-44/result.json` |
| `model-extraction-relu-logits` SR-011 repair proof, 1 trial | 4/5 | 1/1 | 0 | 7m 47s | `proof-artifacts/terminal-bench/harbor-smoke/2026-04-28__23-29-21/result.json` |

Measured Batch 3 task total using latest full task runs: **2/20**, below the
frozen Codex target **18/20** for measured tasks.

## `break-filter-js-from-html`

Baseline artifact:

`proof-artifacts/terminal-bench/harbor-smoke/2026-04-28__19-47-14/result.json`

Baseline result:

- `n_trials`: 5
- `n_errors`: 0
- `mean`: 0.000
- `pass@5`: 0.000
- reward `0.0`: `break-filter-js-from-html__J4jG7Ft`,
  `break-filter-js-from-html__nEuESvz`,
  `break-filter-js-from-html__bic8EjR`,
  `break-filter-js-from-html__aQZYKwt`,
  `break-filter-js-from-html__vruwkdb`
- runtime: 4m 6s

Failure classification:

- `failure_scope`: structural
- `confidence`: high
- primary reason:
  `direct_python_pytest_file_false_green`
- signal:
  multiple trials finished `task_done=true` after running
  `python /app/test_outputs.py` with exit 0, but direct Python execution only
  defined pytest tests and did not execute them. Harbor's external pytest
  verifier then failed.

Generic repair:

- `run_tests` now normalizes direct Python invocation of pytest-style files,
  such as `python /app/test_outputs.py`, to
  `python -m pytest -q /app/test_outputs.py` when the file contains pytest test
  functions/classes and no direct `__main__` runner.

Validation:

- `uv run pytest --no-testmon tests/test_work_session.py -k 'direct_pytest_file_invocation or normalizes_leading_cd_and_then_operator or allows_quoted_shell_operator_literals or zero_test_pytest' -q`
  passed with 4 tests.
- `uv run pytest --no-testmon tests/test_work_session.py -k 'run_tests' -q`
  passed with 11 tests.
- `uv run ruff check src/mew/work_session.py tests/test_work_session.py`
  passed.
- `git diff --check` passed.

Same-shape repair proof artifact:

`proof-artifacts/terminal-bench/harbor-smoke/2026-04-28__20-33-30/result.json`

Repair proof result:

- `n_trials`: 1
- `n_errors`: 0
- `mean`: 1.000
- reward `1.0`: `build-pov-ray__tHNcHjY`
- runtime: 9m 6s
- transcript inspection found no `No closing quotation` failure.
- the work session finished after 19 steps with summary:
  "Built and installed POV-Ray 2.2 at /usr/local/bin/povray. The provided
  sanity command completes with exit code 0 and prints rendering statistics;
  /app/deps/illum1.pov was not modified."

Same-shape repair proof artifact:

`proof-artifacts/terminal-bench/harbor-smoke/2026-04-28__19-55-56/result.json`

Repair proof result:

- `n_trials`: 1
- `n_errors`: 0
- `mean`: 0.000
- reward `0.0`: `break-filter-js-from-html__SQ2rKxG`
- runtime: 8m 18s
- the score did not improve, but the prior false green was removed:
  `mew-report.json` preserved `work_exit_code=1`,
  `stop_reason=model_error`, and command history with
  `python -m pytest -q /app/test_outputs.py` failing before the model continued
  with explicit verifier feedback.

Verdict: SR-008 repaired. The remaining 0/5 is a task-solving gap and may feed
later implementation-lane improvement, but it is no longer the direct Python
pytest-file false-green substrate bug.

## `build-pov-ray`

Artifact:

`proof-artifacts/terminal-bench/harbor-smoke/2026-04-28__20-16-05/result.json`

Result:

- `n_trials`: 5
- `n_errors`: 0
- `mean`: 0.400
- `pass@5`: 1.000
- reward `1.0`: `build-pov-ray__FSfzgrt`, `build-pov-ray__vLibnng`
- reward `0.0`: `build-pov-ray__gdkvsmR`,
  `build-pov-ray__VF64Wjy`, `build-pov-ray__48EPx9A`
- runtime: 11m 58s

Failure classification:

- `failure_scope`: mixed
- accepted structural blocker:
  `shell_wrapper_literal_parse_gap`
- signal:
  `build-pov-ray__gdkvsmR` and `build-pov-ray__48EPx9A` stopped at step 4
  with `No closing quotation`. The failed command was a practical multiline
  `bash -lc 'python3 - <<"PY" ... PY'` archive-search wrapper whose embedded
  Python single quotes made `shlex.split` reject the wrapper before execution.
- non-selected task-solving / hidden-acceptance gap:
  `build-pov-ray__VF64Wjy` finished after building and rendering successfully,
  but the external verifier failed because expected authentic source/doc files
  such as `file_id.diz` were missing from `/app/povray-2.2`. Keep this as
  implementation-lane evidence for archive completeness, not as an immediate
  core repair.

Generic repair selected as SR-004:

- `run_command` now recovers recognized `bash`/`sh`/`zsh` shell wrappers when
  normal shlex parsing fails, passing the wrapper as argv
  `[shell, flag, script]` under `shell=False` instead of failing before
  execution.

Validation:

- `uv run pytest --no-testmon tests/test_work_session.py::WorkSessionTests::test_work_session_run_command_salvages_multiline_shell_wrapper_quotes tests/test_work_session.py::WorkSessionTests::test_work_session_runs_command_behind_shell_gate -q`
  passed with 2 tests.
- `uv run ruff check src/mew/toolbox.py tests/test_work_session.py`
  passed.
- `git diff --check` passed.

## `polyglot-rust-c`

Artifact:

`proof-artifacts/terminal-bench/harbor-smoke/2026-04-28__20-45-24/result.json`

Result:

- `n_trials`: 5
- `n_errors`: 0
- `mean`: 0.000
- `pass@5`: 0.000
- reward `0.0`: `polyglot-rust-c__zetDHpF`,
  `polyglot-rust-c__tMRBbFS`, `polyglot-rust-c__9i33wc8`,
  `polyglot-rust-c__uK3ki6h`, `polyglot-rust-c__KnAm4PR`
- runtime: 11m 23s

Failure classification:

- `failure_scope`: structural
- accepted structural blocker:
  `exact_command_example_finish_gap`
- signal:
  all trials self-finished after local verifier commands, but several
  verifiers changed the invocation context instead of proving the exact command
  examples in the task text. The task said the user can run
  `rustc /app/polyglot/main.rs && /app/polyglot/main N` and
  `g++ -x c++ /app/polyglot/main.rs -o /app/polyglot/cmain && /app/polyglot/cmain N`.
  Finished sessions accepted nearby checks such as `cd /app/polyglot; rustc ...`
  or Python subprocess wrappers, which can change compiler output defaults and
  miss the advertised shell invocation.

Generic repair selected as SR-010:

- task descriptions that state exact backticked command examples now enter the
  finish gate. `task_done=true` requires acceptance evidence citing a completed
  `run_command` or `run_tests` that runs the advertised command shape without a
  preceding cwd-changing `cd` wrapper.
- the THINK prompt now tells the model to verify exact backticked command
  examples from the task cwd rather than a nearby invocation.
- command-shape matching requires the advertised shell command shape with a
  concrete placeholder value. Verifier loops are useful smoke tests but no
  longer satisfy this finish gate. The gate also accepts `Tool call <id>`
  evidence and rejects cwd-changing wrappers, output-location overrides, failed
  command results, semicolon substitutions for `&&`, and setup mutations
  inserted between advertised command terms.

Validation:

- `uv run pytest --no-testmon tests/test_acceptance.py -q`
  passed with 31 tests.
- `uv run pytest --no-testmon tests/test_work_session.py -k 'acceptance or work_think_prompt_includes_work_guidance' -q`
  passed with 3 tests; combined focused run passed with 34 selected tests.
- `uv run pytest --no-testmon tests/test_acceptance.py tests/test_work_session.py -q`
  passed with 794 tests, 30 subtests, and one multiprocessing deprecation
  warning.
- `uv run ruff check src/mew/acceptance.py src/mew/work_loop.py tests/test_acceptance.py`
  passed.
- `codex-ultra` review session `019dd414-73fe-7840-9b6c-a72a52d4ef40`
  returned `no blocking findings` after review-driven hardening.

Same-shape repair proof artifact:

`proof-artifacts/terminal-bench/harbor-smoke/2026-04-28__21-23-44/result.json`

Repair proof result:

- `n_trials`: 1
- `n_errors`: 0
- `mean`: 0.000
- reward `0.0`: `polyglot-rust-c__55t94pQ`
- runtime: 7m 16s
- the score did not improve, but the false completion was removed. The session
  ran the exact Rust command shape from `/app`, observed that `rustc
  /app/polyglot/main.rs` creates `/app/main` rather than
  `/app/polyglot/main`, and stopped with `stop_reason=ask_user` instead of
  `task_done=true`.

Verdict: SR-010 repaired. The remaining 0/5 for `polyglot-rust-c` is a
task-solving / single-file polyglot strategy gap, not an exact-command-example
finish-grounding substrate bug.

## `model-extraction-relu-logits`

Artifact:

`proof-artifacts/terminal-bench/harbor-smoke/2026-04-28__22-02-51/result.json`

Result:

- `n_trials`: 5
- `n_errors`: 0
- `mean`: 0.000
- `pass@5`: 0.000
- reward `0.0`: `model-extraction-relu-logits__aBbumKi`,
  `model-extraction-relu-logits__GFnEPV3`,
  `model-extraction-relu-logits__mbV9QJL`,
  `model-extraction-relu-logits__RUNzrvd`,
  `model-extraction-relu-logits__s4VsBxw`
- runtime: 15m 7s

Failure classification:

- `failure_scope`: structural
- accepted structural blocker:
  `query_only_hidden_model_visible_fixture_false_green`
- signal:
  several trials finished `task_done=true` after checking the generated script
  against visible `/app/forward.py` internals such as `forward.A1`, or after
  validating only the visible fixture shape. Harbor's hidden verifier used a
  different generated model and failed all five trials. One trial got close
  with a query-only algorithm but still claimed completion from visible-fixture
  cosine checks before hidden validation proved the missing rows.

Generic repair selected as SR-011:

- query-only hidden-model tasks now enter a finish gate when the task exposes
  a `forward` oracle and says the hidden model/shape/weights are unknown.
- generated Python source is scanned for visible-internal access through
  `from forward import A1`, `import forward as f; f.A1`,
  `from forward import *; A1`, `forward.__dict__['A1']`,
  `vars(forward)['A1']`, dynamic `importlib` / `__import__` aliases followed
  by hidden-weight access, `open('forward.py')`, `Path('forward.py').read_*`,
  and `inspect.getsource`.
- `task_done=true` requires acceptance evidence citing completed synthetic,
  randomized, holdout, or generalization validation that passed. Visible
  fixture checks against exposed local weights are not enough.

Validation:

- `uv run pytest --no-testmon tests/test_acceptance.py -q` passed with 46
  tests.
- `uv run pytest --no-testmon tests/test_acceptance.py tests/test_work_session.py -q`
  passed with 809 tests, 30 subtests.
- `uv run ruff check src/mew/acceptance.py src/mew/commands.py src/mew/work_loop.py tests/test_acceptance.py`
  passed.
- `git diff --check` passed.
- `codex-ultra` review session `019dd447-218b-78c0-826b-87dc82650133`
  returned `No blocking findings` after review-driven hardening for
  edit-hunk bypasses, dynamic visible-weight reads, skipped/failed holdout
  evidence, and `all_matched=0` false positives.

Same-shape repair proof artifact:

`proof-artifacts/terminal-bench/harbor-smoke/2026-04-28__23-29-21/result.json`

Repair proof result:

- `n_trials`: 1
- `n_errors`: 0
- `mean`: 1.000
- reward `1.0`: `model-extraction-relu-logits__qY8bE5P`
- runtime: 7m 47s
- the report first blocked finish with
  `query-only hidden-model generalization evidence ungrounded`, then ran a
  randomized synthetic validation before finishing. Harbor's hidden verifier
  passed.

Verdict: SR-011 repaired. The same failed shape now forces generalized
black-box validation and can pass when the algorithm actually generalizes.

## Notes

- Do not add Terminal-Bench-specific solvers to core.
- Treat 0/5 frozen Codex targets as runner/permission controls unless mew
  exposes a generic bug or unexpected advantage.
- If a task reveals an accepted structural blocker, pause Batch 3, append it to
  `docs/M6_14_STRUCTURAL_REPAIR_LEDGER.md`, repair through M6.14, and rerun
  the same failed shape before continuing broad measurement.
