# Generated Demo Assets

This directory is the output target for VHS demo tapes under `scripts/`.

Generated GIFs are ignored by git by default. Regenerate them locally:

```sh
vhs scripts/demo_primary.tape
vhs scripts/demo_short.tape
```
